import 'package:flutter/material.dart';

class LevelInfo{
      LevelInfo({required this.unClock,required this.images, required this.title});
      IconData? unClock;
      String images;
      String title;
}